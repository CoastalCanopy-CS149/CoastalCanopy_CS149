# from flask import Blueprint, jsonify
# from app import mongo
#
# reports_bp = Blueprint("reports", __name__)
#
# @reports_bp.route("/add", methods=["POST"])
# def add_report():
#     return jsonify({"message": "Report added!"}), 201
